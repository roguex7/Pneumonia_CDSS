# ─────────────────────────────────────────────────────────────────────────────
# Pneumonia CDSS – Python Dependencies
# Strategy: >=current_stable, <next_major
#
# Why this range?
#   ✅  Automatically receives patch + minor releases (bug fixes, CVE patches)
#   ✅  Blocked from silent breaking changes when a new major ships
#   ✅  Reproducible across Streamlit Cloud re-deploys
#   ✅  Safe to upgrade deliberately: bump lower bound after testing
#
# Last reviewed: March 2026
# To regenerate a full lockfile: pip-compile requirements.txt --strip-extras
# ─────────────────────────────────────────────────────────────────────────────

# ── Core ML / Inference ──────────────────────────────────────────────────────
# Latest stable: torch 2.10.0 / torchvision 0.25.0 (Jan 2026)
# torch and torchvision MUST be upgraded together — compatibility matrix:
# https://github.com/pytorch/pytorch/wiki/PyTorch-Versions
torch>=2.10,<3
torchvision>=0.25,<1

# ── Computer Vision ──────────────────────────────────────────────────────────
# ✅ PERMANENT FIX: headless build — eliminates libgthread/libGL/libGTK deps
# Identical API to opencv-python; correct build for any server/cloud deployment
opencv-python-headless>=4.8,<5

# ── Numerical / Data ─────────────────────────────────────────────────────────
# numpy capped at <2: NumPy 2.x has breaking C-API changes; wait for
# YOLOv5 upstream to confirm NumPy 2 support before lifting this cap
numpy>=1.24,<2
pandas>=2.0,<3
scipy>=1.10,<2

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.7,<4
seaborn>=0.12,<1

# ── YOLOv5 Runtime ───────────────────────────────────────────────────────────
Pillow>=10.0,<12
PyYAML>=6.0,<7
requests>=2.28,<3
tqdm>=4.65,<5

# ── Streamlit App ────────────────────────────────────────────────────────────
# Latest stable: 1.43.x (March 2026)
streamlit>=1.28,<2
